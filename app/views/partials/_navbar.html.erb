<nav class="navbar is-dark is-transparent">
  <div class="navbar-brand">
    <%= link_to "SENSE", root_path, class: 'navbar-item' %>
    <div class="navbar-burger burger" data-target="navbarExampleTransparentExample">
      <span></span>
      <span></span>
      <span></span>
    </div>
  </div>

  <div id="navbarExampleTransparentExample" class="navbar-menu">
    <div class="navbar-start">
      <%= link_to "Home", root_path, class: 'navbar-item' %>
      <%= link_to "Headlines", article_headlines_path, class: 'navbar-item' %>
    </div>

    <div class="navbar-end">
      <div class="navbar-item">
        <div class="buttons">
            <% if user_signed_in? %>
                <%= link_to "New Post", new_post_path, class: 'button is-warning' %>
                <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: 'button is-info' %>
                <%= link_to "Edit Profile", edit_user_registration_path, class: 'button is-light' %>
            <% else %>
                <%= link_to "Sign up", new_user_registration_path, class: 'button is-link' %>
                <%= link_to "Log in", new_user_session_path, class: 'button is-primary' %>
            <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>